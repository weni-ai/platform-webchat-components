# platform-webchat-components

Vue component library for Weni CX Platform webchat experiences (Agent Builder and Live Desk).

This repository is initialized with [Spec Kit](https://github.com/github/spec-kit). Start with `/speckit-constitution`, then `/speckit-specify`.
